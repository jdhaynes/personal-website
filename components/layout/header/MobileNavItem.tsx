import React from "react";
import { NavLink } from "@/components/layout/header/Header";

const MobileNavItem: React.FC<NavLink> = ({ name, href }) => {
  return <div></div>;
};

export default MobileNavItem;
